---
- name: local playbook
  hosts: localhost
  connection: local

  roles:
    - ../roles/alacritty
    - ../roles/apt
    - ../roles/emacs
    - ../roles/fontconfig
    - ../roles/git
    - ../roles/neovim
    - ../roles/pipx
    - ../roles/starship
    - ../roles/xinitrc
